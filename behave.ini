[behave]
# Define the location of the features and steps directories
paths = features
# Define the default formatter (you can change this based on your preference)
format = progress

# Define the default language for feature files
lang = en

# Specify additional options as needed
# e.g., uncomment the following line to generate junit reports
# junit=true
